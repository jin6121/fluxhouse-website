---
import Layout from '../layouts/Layout.astro';

interface NoteItem {
  title: string;
  link: string;
  pubDate: string;
  description: string;
}

let noteItems: NoteItem[] = [];
try {
  const res = await fetch('https://note.com/jin_sariimama/m/mbada4e66a2a3/rss');
  const xml = await res.text();
  const items = xml.match(/<item>([\s\S]*?)<\/item>/g) || [];
  noteItems = items.slice(0, 3).map((item) => {
    const title = item.match(/<title><!\[CDATA\[(.*?)\]\]><\/title>/)?.[1] || item.match(/<title>(.*?)<\/title>/)?.[1] || '';
    const link = item.match(/<link>(.*?)<\/link>/)?.[1] || '';
    const pubDate = item.match(/<pubDate>(.*?)<\/pubDate>/)?.[1] || '';
    const description = item.match(/<description><!\[CDATA\[(.*?)\]\]><\/description>/)?.[1] || item.match(/<description>(.*?)<\/description>/)?.[1] || '';
    const date = pubDate ? new Date(pubDate).toLocaleDateString('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' }) : '';
    const plainDesc = description.replace(/<[^>]+>/g, '').slice(0, 80);
    return { title, link, pubDate: date, description: plainDesc };
  });
} catch (e) {
  noteItems = [];
}
---

<!-- TODO: OGP画像をプロ撮影写真に差し替え後、ogImage を更新 -->
<Layout
  title="FLUX HOUSE | 東京から30分、一日貸切のハウススタジオ＆オフサイト拠点（千葉・南流山）"
  description="撮影・チーム合宿のための一棟貸切ハウス。秋葉原からTXで約25分。自然光のリビングとキッチンを備えた一軒家を、一日まるごと。FLUX DAY ¥100,000/10時間。"
  ogUrl="https://fluxhouse-website.pages.dev/"
>

<!-- ========== HEADER ========== -->
<header class="site-header">
  <div class="header-inner container">
    <a href="/" class="logo">
      <img src="/fluxhouse_logo_yoko_trimmed.png" alt="FLUX HOUSE" class="logo-img" />
    </a>
    <nav class="nav-desktop" aria-label="メインナビゲーション">
      <a href="#offsite">OFFSITE</a>
      <a href="#shooting">SHOOTING</a>
      <a href="#pricing">PRICING</a>
      <a href="#access">ACCESS</a>
      <a href="#contact" class="nav-cta">見学を予約する</a>
    </nav>
    <button class="hamburger" aria-label="メニューを開く" aria-expanded="false" aria-controls="mobile-menu">
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="mobile-menu" id="mobile-menu" aria-hidden="true">
    <nav>
      <a href="#offsite" class="mobile-nav-link">OFFSITE</a>
      <a href="#shooting" class="mobile-nav-link">SHOOTING</a>
      <a href="#pricing" class="mobile-nav-link">PRICING</a>
      <a href="#access" class="mobile-nav-link">ACCESS</a>
      <a href="#contact" class="mobile-nav-link">見学を予約する</a>
    </nav>
  </div>
</header>

<!-- ========== HERO ========== -->
<!-- PHOTO: リビング全体の引き。自然光が入る昼間。人物なし。横位置。 -->
<section class="hero">
  <div class="hero-bg"></div>
    <p class="hero-label fade-in">A HOUSE FOR TEAMS &amp; CREATORS &nbsp;—&nbsp; 30 min from Tokyo</p>
    <h1 class="hero-heading fade-in">会議室では、<br />生まれない。</h1>
    <p class="hero-desc fade-in">
      一軒家を一日、チームのものに。<br />
      FLUX HOUSEは、東京から30分の"考える家"。<br />
      撮影に、合宿に、まだ名前のないプロジェクトに。
    </p>
    <div class="hero-cta fade-in">
      <a href="#contact" class="btn-primary-dark">見学を予約する</a>
      <a href="#contact" class="btn-ghost">空き状況を問い合わせる</a>
    </div>
  </div>
</section>

<!-- ========== 2 WAYS ========== -->
<section class="section bg-white" id="ways">
  <div class="container">
    <div class="ways-grid">
      <a href="#offsite" class="way-card fade-in">
        <p class="way-label">OFFSITE</p>
        <h2 class="way-heading">オフサイト・合宿</h2>
        <p class="way-body">
          オフィスを離れて、家で考える。秋葉原からTXで約25分。
          キッチンを囲み、リビングで議論する一日が、チームの空気を変えます。
        </p>
        <span class="way-link">詳細を見る
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
      </a>
      <a href="#shooting" class="way-card fade-in">
        <p class="way-label">SHOOTING</p>
        <h2 class="way-heading">撮影利用</h2>
        <p class="way-body">
          生活の気配が残る一軒家。自然光のリビング、使えるキッチン、搬入しやすい動線。
          広告・EC・映像の撮影現場として、一日まるごと。
        </p>
        <span class="way-link">スペック詳細を見る
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
      </a>
    </div>
  </div>
</section>

<!-- ========== CONCEPT ========== -->
<section class="section bg-warm" id="concept">
  <div class="container layout-text">
    <div class="sec-title fade-in">
      <p class="sec-en">CONCEPT</p>
      <h2 class="sec-ja">コンセプト</h2>
    </div>
    <div class="text-body fade-in">
      <p>
        FLUX——流れ。人が交わるたびに、かたちを変える場所。
      </p>
      <p>
        FLUX HOUSEは、会議室にもスタジオにも、合宿所にもなる一軒の家です。
        都市と自然が混ざり合う南流山で、チームの一日に"暮らし"の温度を。
      </p>
      <p>
        「借りる」のではなく、「占有する」一日。そこから生まれるものに、
        名前はまだなくていい。
      </p>
      <a href="https://note.com/jin_sariimama/m/mbada4e66a2a3" target="_blank" rel="noopener noreferrer" class="text-link">
        コンセプトについて詳しく読む →
      </a>
    </div>
  </div>
</section>

<!-- ========== PRICING ========== -->
<section class="section bg-white" id="pricing">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">PRICING</p>
      <h2 class="sec-ja">料金</h2>
    </div>
    <div class="pricing-grid fade-in">

      <div class="pricing-card pricing-card--featured">
        <div class="pricing-card-head">
          <p class="pricing-plan">FLUX DAY</p>
          <p class="pricing-sub">一棟完全貸切 / 10時間</p>
        </div>
        <p class="pricing-price">¥100,000<span class="pricing-tax">（税込）</span></p>
        <ul class="pricing-includes">
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            一棟完全貸切
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            キッチン・調理器具
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            高速Wi-Fi / 冷暖房
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            撮影利用権（商用可）
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            家具レイアウト変更可
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            駐車場
          </li>
        </ul>
      </div>

      <div class="pricing-card">
        <div class="pricing-card-head">
          <p class="pricing-plan">HALF DAY</p>
          <p class="pricing-sub">一棟完全貸切 / 4時間</p>
        </div>
        <p class="pricing-price">¥60,000<span class="pricing-tax">（税込）</span></p>
        <ul class="pricing-includes">
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            FLUX DAYと同内容
          </li>
          <li>
            <svg class="check-icon" width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M2 7l3.5 3.5L12 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            午前 or 午後の選択制
          </li>
        </ul>
      </div>

    </div>

    <div class="pricing-notes fade-in">
      <p>延長 ¥15,000 / 時</p>
      <p>下見・ロケハンは無料（要予約）</p>
    </div>

    <div class="fade-in" style="margin-top: 56px;">
      <a href="#contact" class="btn-outline">見学・下見を予約する</a>
    </div>

    <!-- 家主滞在案内 -->
    <div class="host-presence fade-in">
      <div class="host-presence-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
          <circle cx="10" cy="10" r="8.5" stroke="currentColor" stroke-width="1.2"/>
          <path d="M10 9v5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
          <circle cx="10" cy="6.5" r="0.8" fill="currentColor"/>
        </svg>
      </div>
      <div class="host-presence-body">
        <p class="host-presence-heading">運営者が在館しています</p>
        <p class="host-presence-text">
          FLUX HOUSEは、運営者が同じ建物内に滞在する「家主滞在型」の施設です。
          設備の使い方やキッチン・BBQの段取り、撮影時の備品移動など、
          困ったときにすぐ相談できる体制でお迎えします。
        </p>
        <p class="host-presence-text">
          セッションや撮影の進行を妨げることはありません。
          プライバシーに配慮し、必要なとき以外はお声がけいたしません。
        </p>
        <p class="host-presence-note">※貸切は一棟・一社単位です。同時に複数の組をお受けすることはありません。</p>
      </div>
    </div>
  </div>
</section>

<!-- ========== OFFSITE ========== -->
<section class="section bg-warm" id="offsite">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">OFFSITE</p>
      <h2 class="sec-ja">オフサイト・合宿として</h2>
    </div>

    <div class="offsite-grid">
      <div class="photo-placeholder offsite-photo fade-in" data-caption="テーブルを囲む4〜6名／自然光・俯瞰または斜めから">
        <img src="https://images.unsplash.com/photo-1497366216548-37526070297c?w=800&q=80" alt="撮影ダミー：オフサイト" class="gallery-img" />
        <span class="photo-badge">要撮影</span>
      </div>
      <div class="offsite-info fade-in">
        <p class="offsite-body">
          オフィスの外で、チームが集まる。フラットな空間で生まれる議論は、
          会議室のそれとは質が違います。FLUX HOUSEは、考える場所であると同時に、
          食べて、話して、BBQで締めくくれる場所でもあります。
        </p>
        <dl class="offsite-spec">
          <dt>推奨人数</dt>
          <dd>4〜10名</dd>
          <dt>設備</dt>
          <!-- TODO: ホワイトボード・モニター・コーヒーセットの保有状況を確認して更新 -->
          <dd>高速Wi-Fi / キッチン・調理器具 / 冷暖房 / 大型テーブル / 2階ベランダ（BBQ可）/ ホワイトボード（要確認）/ プロジェクター or モニター（要確認）/ コーヒーセットアップ（要確認）</dd>
          <dt>アクセス</dt>
          <dd>秋葉原からTX快速で約25分 + 徒歩 or 車</dd>
        </dl>
      </div>

      <div class="schedule fade-in">
        <h3 class="schedule-heading">モデルスケジュール</h3>
        <p class="schedule-note">例：10:00〜21:00 / 11:00〜21:00</p>
        <ol class="schedule-list">
          <li>
            <span class="schedule-time">10:00</span>
            <span class="schedule-item">先乗り組チェックイン・買い出し（近隣スーパーへ）</span>
          </li>
          <li>
            <span class="schedule-time">11:00</span>
            <span class="schedule-item">全員集合</span>
          </li>
          <li>
            <span class="schedule-time">11:15</span>
            <span class="schedule-item">全体MTG ── 今日のゴール確認・議題共有</span>
          </li>
          <li>
            <span class="schedule-time">12:00</span>
            <span class="schedule-item">個別 / チーム別MTG ── テーマごとに分かれて深掘り</span>
          </li>
          <li>
            <span class="schedule-time">13:30</span>
            <span class="schedule-item">ランチ ── 外食 / デリバリー（Uber Eats対応エリア）/ キッチンで一緒に作る</span>
          </li>
          <li>
            <span class="schedule-time">14:30</span>
            <span class="schedule-item">午後セッション ── 午前の続き</span>
          </li>
          <li>
            <span class="schedule-time">16:30</span>
            <span class="schedule-item">まとめ・ネクストアクション整理</span>
          </li>
          <li>
            <span class="schedule-time">17:30</span>
            <span class="schedule-item">BBQ準備</span>
          </li>
          <li>
            <span class="schedule-time">18:00</span>
            <span class="schedule-item">BBQスタート ── 2階ベランダで火を囲みながら</span>
          </li>
          <li>
            <span class="schedule-time">21:00</span>
            <span class="schedule-item">チェックアウト</span>
          </li>
        </ol>
        <p class="schedule-footnote">※BBQはベランダ設備を使用します。食材・機材の手配方法はお問い合わせください。</p>
      </div>
    </div>
  </div>
</section>

<!-- ========== SHOOTING STUDIO SPEC ========== -->
<section class="section bg-white" id="shooting">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">SHOOTING</p>
      <h2 class="sec-ja">撮影スタジオとして</h2>
    </div>

    <!-- TODO: プロ撮影写真に差し替え -->
    <div class="gallery fade-in">
      <div class="gallery-main">
        <div class="photo-placeholder" data-caption="リビング・自然光／窓を背にした引きのカット">
          <img src="https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=800&q=80" alt="撮影ダミー：リビング" class="gallery-img" />
          <span class="photo-badge">要撮影</span>
        </div>
      </div>
      <div class="gallery-sub">
        <div class="photo-placeholder" data-caption="キッチン全体／作業面と窓が両方入るアングル">
          <img src="https://images.unsplash.com/photo-1556909114-f6e7ad7d3136?w=800&q=80" alt="撮影ダミー：キッチン" class="gallery-img" />
          <span class="photo-badge">要撮影</span>
        </div>
        <div class="photo-placeholder" data-caption="外観・玄関／搬入経路がわかるアングル">
          <img src="https://images.unsplash.com/photo-1449824913935-59a10b8d2000?w=800&q=80" alt="撮影ダミー：外観" class="gallery-img" />
          <span class="photo-badge">要撮影</span>
        </div>
      </div>
    </div>

    <div class="spec-wrap fade-in">
      <h3 class="spec-heading">スペック</h3>
      <table class="spec-table">
        <tbody>
          <tr>
            <th>延床面積</th>
            <!-- TODO: 実測値に差し替え -->
            <td>要確認</td>
          </tr>
          <tr>
            <th>リビング・ダイニング</th>
            <!-- TODO: 実測値に差し替え -->
            <td>要確認</td>
          </tr>
          <tr>
            <th>天井高</th>
            <!-- TODO: 実測値に差し替え -->
            <td>要確認</td>
          </tr>
          <tr>
            <th>自然光</th>
            <!-- TODO: 窓の向き・良好な時間帯を確認して記載 -->
            <td>要確認（窓の方角・良好な撮影時間帯）</td>
          </tr>
          <tr>
            <th>電源容量</th>
            <!-- TODO: 電源アンペア数・コンセント位置の確認 -->
            <td>要確認（アンペア数・コンセント位置）</td>
          </tr>
          <tr>
            <th>搬入経路</th>
            <!-- TODO: 駐車場から玄関までの距離・段差確認 -->
            <td>要確認（駐車場〜玄関、段差の有無）</td>
          </tr>
          <tr>
            <th>控室・更衣スペース</th>
            <!-- TODO: 有無と広さを確認 -->
            <td>要確認</td>
          </tr>
          <tr>
            <th>家具の移動</th>
            <td>可</td>
          </tr>
          <tr>
            <th>撮影利用</th>
            <td>商用可（広告・EC・映像・雑誌 等）</td>
          </tr>
          <tr>
            <th>飲食</th>
            <td>可（キッチン使用可）</td>
          </tr>
        </tbody>
      </table>

      <!-- TODO: 間取り図画像を配置 -->
      <div class="floorplan-ph fade-in">
        <p>間取り図</p>
        <!-- TODO: 間取り図画像（/floorplan.png）に差し替え -->
      </div>
    </div>
  </div>
</section>

<!-- ========== CASE STUDIES ========== -->
<section class="section bg-warm" id="cases">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">CASE</p>
      <h2 class="sec-ja">利用事例</h2>
    </div>
    <!-- TODO: モニター利用2〜3件を獲得後に実際の事例に差し替え -->
    <div class="case-grid">
      <div class="case-card fade-in">
        <div class="case-ph">
          <!-- TODO: 事例写真に差し替え -->
        </div>
        <p class="case-tag">撮影利用</p>
        <p class="case-title">導入事例は順次掲載予定</p>
        <p class="case-body">モニター利用を随時受付中。詳細はお問い合わせください。</p>
      </div>
      <div class="case-card fade-in">
        <div class="case-ph">
          <!-- TODO: 事例写真に差し替え -->
        </div>
        <p class="case-tag">オフサイト利用</p>
        <p class="case-title">導入事例は順次掲載予定</p>
        <p class="case-body">モニター利用を随時受付中。詳細はお問い合わせください。</p>
      </div>
      <div class="case-card fade-in">
        <div class="case-ph">
          <!-- TODO: 事例写真に差し替え -->
        </div>
        <p class="case-tag">撮影 / オフサイト</p>
        <p class="case-title">導入事例は順次掲載予定</p>
        <p class="case-body">モニター利用を随時受付中。詳細はお問い合わせください。</p>
      </div>
    </div>
  </div>
</section>

<!-- ========== OPERATOR ========== -->
<section class="section bg-white" id="about">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">ABOUT</p>
      <h2 class="sec-ja">運営者について</h2>
    </div>
    <div class="operators-wrap">
      <div class="operator-card fade-in">
        <div class="photo-placeholder operator-placeholder" data-caption="運営者ポートレート／自然光・屋内・柔らかい表情">
          <img src="https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?w=400&q=80" alt="撮影ダミー：永妻寛哲" class="gallery-img" />
          <span class="photo-badge">要撮影</span>
        </div>
        <div class="operator-info">
          <p class="operator-name">永妻 寛哲</p>
          <p class="operator-role">ワークスタイルコンサルティング合同会社 代表社員 / FLUX HOUSE 共同運営</p>
          <p class="operator-bio">
            千葉県松戸市出身。立教大学経済学部卒。
            アソビュー株式会社、ドクターメイト株式会社をシード期から立ち上げに参画。
            エンジニアなしでシステムを構築し、事業を億単位の売上規模へ成長させた経験を持つ。
          </p>
          <p class="operator-bio">
            2017年に独立・起業。ITやクラウドの活用支援を軸に、スタートアップから中小企業まで幅広くコンサルティングを行う。
            スタートアップで培った「場の熱量が事業を動かす」という確信から、南流山にFLUX HOUSEを開設。
          </p>
          <p class="operator-link">
            <a href="https://workstyle-c.com/" target="_blank" rel="noopener noreferrer" class="text-link">
              ワークスタイルコンサルティング合同会社 →
            </a>
          </p>
        </div>
      </div>

      <div class="operator-card fade-in">
        <div class="photo-placeholder operator-placeholder" data-caption="運営者ポートレート／自然光・屋内・柔らかい表情">
          <img src="https://images.unsplash.com/photo-1580489944761-15a19d654956?w=400&q=80" alt="撮影ダミー：神徳あや" class="gallery-img" />
          <span class="photo-badge">要撮影</span>
        </div>
        <div class="operator-info">
          <p class="operator-name">神徳 あや</p>
          <p class="operator-role">FLUX HOUSE 共同運営</p>
          <p class="operator-bio">
            経営コンサルティング会社にて18年間、ディレクター職を経て独立。流山市在住。
            「南流山という土地でしか生まれない体験を届けたい」という思いから、FLUX HOUSEを開設。
          </p>
          <p class="operator-bio">
            法人の現場制作・チームビルディングの支援経験を持つ。
            利用目的に合わせた活用方法を一緒に考えます。
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ========== ACCESS ========== -->
<section class="section bg-warm" id="access">
  <div class="container">
    <div class="sec-title fade-in">
      <p class="sec-en">ACCESS</p>
      <h2 class="sec-ja">アクセス</h2>
    </div>

    <div class="access-highlight fade-in">
      <p>秋葉原からTX快速で約25分 + 徒歩 or 車</p>
      <p>都内からロケバス1台でアクセス可</p>
    </div>

    <div class="map-toggle fade-in">
      <button class="map-btn map-btn--active" data-lang="ja" aria-pressed="true">日本語</button>
      <button class="map-btn" data-lang="en" aria-pressed="false">English</button>
    </div>

    <div class="map-image-wrap fade-in">
      <img
        id="map-img-ja"
        src="/access-map-japanese.png"
        alt="アクセスマップ（日本語）"
        class="map-image"
        style="display:block;"
      />
      <img
        id="map-img-en"
        src="/access-map-english.png"
        alt="Access map (English)"
        class="map-image"
        style="display:none;"
      />
    </div>

    <ul class="access-info-list fade-in">
      <li>つくばエクスプレス・JR武蔵野線「南流山駅」から徒歩15分・車5分</li>
      <li>秋葉原駅からTX快速で約25分（南流山駅下車）</li>
      <li>お車でお越しの場合は駐車場についてお問い合わせください</li>
    </ul>

    <div class="area-map-wrap fade-in">
      <img
        src="/FLUX HOUSE minaminagareyama.png"
        alt="南流山エリアマップ"
        class="area-map-img"
      />
    </div>
  </div>
</section>

<!-- ========== CONTACT ========== -->
<section class="section bg-dark" id="contact">
  <div class="container">
    <div class="contact-inner fade-in">
      <div class="sec-title">
        <p class="sec-en sec-en--light">CONTACT</p>
        <h2 class="sec-ja sec-ja--light">見学・下見のご予約</h2>
      </div>
      <p class="contact-text">
        下見・ロケハンは無料でお受けしています（要予約）。<br />
        まずは現地をご覧ください。<br />
        <br />
        通常2〜3営業日以内にご返信いたします。
      </p>
      <div class="contact-cta">
        <a href="https://forms.gle/MAnhrqk2x8Rn2JWD7" target="_blank" rel="noopener noreferrer" class="btn-primary-light">
          見学を予約する
        </a>
        <a href="https://forms.gle/MAnhrqk2x8Rn2JWD7" target="_blank" rel="noopener noreferrer" class="btn-ghost-light">
          空き状況を問い合わせる
        </a>
      </div>
      <div class="contact-legal">
        <!-- TODO: 各ページを作成後リンクを有効化 -->
        <a href="/tokushoho" class="legal-link">特定商取引法に基づく表記</a>
        <a href="/cancel-policy" class="legal-link">キャンセルポリシー</a>
      </div>
    </div>
  </div>
</section>

<!-- ========== FOOTER ========== -->
<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-top">
      <div class="footer-logo">
        <img src="/fluxhouse_logo_tate_white.png" alt="FLUX HOUSE" class="footer-logo-img" />
      </div>
      <nav class="footer-links">
        <a href="https://www.instagram.com/fluxhouse_nagareyama/" target="_blank" rel="noopener noreferrer">Instagram</a>
        <a href="https://note.com/jin_sariimama/m/mbada4e66a2a3" target="_blank" rel="noopener noreferrer">note</a>
        <a href="#contact">お問い合わせ</a>
        <a href="/local" class="footer-link-sub">地域の方へ（FLUX LOCAL）</a>
      </nav>
    </div>

    <!-- STAY mini -->
    <div class="stay-mini">
      <span class="stay-badge">Coming Soon</span>
      <p class="stay-text">民泊・住宅宿泊事業 準備中 — <a href="https://www.instagram.com/fluxhouse_nagareyama/" target="_blank" rel="noopener noreferrer">Instagram</a> でお知らせ予定</p>
    </div>

    <p class="footer-copy">©2026 FLUX HOUSE / ワークスタイルコンサルティング合同会社</p>
  </div>
</footer>

</Layout>

<style>
  /* ============================================================
     BACKGROUNDS
     ============================================================ */
  .bg-white { background: #ffffff; }
  .bg-warm  { background: #F7F4EF; }
  .bg-dark  { background: #1a1a1a; }

  /* ============================================================
     SECTION BASE
     ============================================================ */
  .section {
    padding: 96px 0;
  }

  @media (min-width: 768px) {
    .section {
      padding: 160px 0;
    }
  }

  /* ============================================================
     SECTION TITLE
     ============================================================ */
  .sec-title {
    margin-bottom: 64px;
  }

  .sec-en {
    font-family: var(--font-body);
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #999;
    margin-bottom: 12px;
  }

  .sec-en--light {
    color: rgba(255,255,255,0.45);
  }

  .sec-ja {
    font-family: var(--font-heading);
    font-size: clamp(1.6rem, 4vw, 2.4rem);
    font-weight: 700;
    color: #1a1a1a;
    line-height: 1.3;
  }

  .sec-ja--light {
    color: #ffffff;
  }

  /* ============================================================
     HEADER
     ============================================================ */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(255, 255, 255, 0.94);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(0,0,0,0.07);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 80px;
  }

  @media (min-width: 768px) {
    .header-inner { height: 96px; }
  }

  .logo { display: flex; align-items: center; }

  .logo-img {
    height: 36px;
    width: auto;
    display: block;
    mix-blend-mode: multiply;
  }

  @media (min-width: 768px) {
    .logo-img { height: 48px; }
  }

  .nav-desktop {
    display: none;
    align-items: center;
    gap: 36px;
  }

  .nav-desktop a {
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    color: #1a1a1a;
    text-transform: uppercase;
    position: relative;
    transition: color 0.3s ease;
  }

  .nav-desktop a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background: #8B6F47;
    transition: width 0.3s ease;
  }

  .nav-desktop a:hover { color: #8B6F47; }
  .nav-desktop a:hover::after { width: 100%; }

  .nav-cta {
    border: 1px solid #1a1a1a !important;
    padding: 8px 20px !important;
    border-radius: 2px;
    font-size: 0.7rem !important;
    transition: background 0.3s ease, color 0.3s ease, border-color 0.3s ease !important;
  }

  .nav-cta::after { display: none !important; }

  .nav-cta:hover {
    background: #1a1a1a !important;
    color: #ffffff !important;
    border-color: #1a1a1a !important;
  }

  @media (min-width: 768px) {
    .nav-desktop { display: flex; }
    .hamburger   { display: none; }
  }

  .hamburger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 40px;
    height: 40px;
    padding: 8px;
    background: none;
    border: none;
    cursor: pointer;
  }

  .hamburger span {
    display: block;
    width: 22px;
    height: 1.5px;
    background: #1a1a1a;
    border-radius: 2px;
    transition: transform 0.3s ease, opacity 0.3s ease;
  }

  .hamburger.open span:nth-child(1) { transform: translateY(6.5px) rotate(45deg); }
  .hamburger.open span:nth-child(2) { opacity: 0; }
  .hamburger.open span:nth-child(3) { transform: translateY(-6.5px) rotate(-45deg); }

  .mobile-menu {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease;
    background: rgba(255,255,255,0.98);
    border-top: 1px solid rgba(0,0,0,0.06);
  }

  .mobile-menu.open { max-height: 360px; }

  .mobile-menu nav {
    display: flex;
    flex-direction: column;
    padding: 16px 24px 28px;
  }

  .mobile-nav-link {
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #1a1a1a;
    padding: 14px 0;
    border-bottom: 1px solid rgba(0,0,0,0.07);
    transition: color 0.3s ease;
  }

  .mobile-nav-link:last-child { border-bottom: none; }
  .mobile-nav-link:hover { color: #8B6F47; }

  /* ============================================================
     HERO
     ============================================================ */
  .hero {
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #1a1a1a;
    padding: 120px 24px 100px;
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background-image: url('https://images.unsplash.com/photo-1586023492125-27b2c045efd7?w=1600&q=80');
    background-size: cover;
    background-position: center;
    opacity: 0.35;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(to bottom, rgba(26,26,26,0.5) 0%, rgba(26,26,26,0.3) 50%, rgba(26,26,26,0.6) 100%),
      radial-gradient(ellipse 60% 60% at 20% 70%, rgba(139,111,71,0.25) 0%, transparent 60%);
    pointer-events: none;
    z-index: 1;
  }

  .hero-inner {
    position: relative;
    z-index: 2;
    max-width: 780px;
    width: 100%;
  }

  .hero-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
    margin-bottom: 32px;
    font-family: var(--font-body);
  }

  .hero-heading {
    font-family: 'Noto Serif JP', 'Shippori Mincho', serif;
    font-size: clamp(3rem, 10vw, 6.5rem);
    font-weight: 700;
    color: #ffffff;
    line-height: 1.2;
    letter-spacing: 0.02em;
    margin-bottom: 36px;
  }

  .hero-desc {
    font-size: clamp(0.9rem, 1.8vw, 1.05rem);
    color: rgba(255,255,255,0.65);
    line-height: 2;
    letter-spacing: 0.04em;
    margin-bottom: 64px;
  }

  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
  }

  .btn-primary-dark {
    display: inline-block;
    background: #ffffff;
    color: #1a1a1a;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    padding: 16px 40px;
    border-radius: 2px;
    transition: background 0.3s ease, color 0.3s ease, transform 0.3s ease;
  }

  .btn-primary-dark:hover {
    background: #8B6F47;
    color: #ffffff;
    transform: translateY(-2px);
  }

  .btn-ghost {
    display: inline-block;
    border: 1px solid rgba(255,255,255,0.4);
    color: rgba(255,255,255,0.8);
    font-size: 0.875rem;
    font-weight: 400;
    letter-spacing: 0.08em;
    padding: 16px 40px;
    border-radius: 2px;
    transition: border-color 0.3s ease, color 0.3s ease, transform 0.3s ease;
  }

  .btn-ghost:hover {
    border-color: rgba(255,255,255,0.8);
    color: #ffffff;
    transform: translateY(-2px);
  }

  /* ============================================================
     2 WAYS
     ============================================================ */
  .ways-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2px;
  }

  @media (min-width: 768px) {
    .ways-grid { grid-template-columns: 1fr 1fr; }
  }

  .way-card {
    display: block;
    padding: 64px 48px;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 4px;
    transition: box-shadow 0.3s ease, transform 0.3s ease;
    text-decoration: none;
  }

  .way-card:hover {
    box-shadow: 0 8px 40px rgba(0,0,0,0.08);
    transform: translateY(-4px);
  }

  @media (max-width: 767px) {
    .way-card { padding: 48px 32px; }
  }

  .way-label {
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: #999;
    margin-bottom: 16px;
  }

  .way-heading {
    font-family: var(--font-heading);
    font-size: clamp(1.4rem, 3vw, 1.8rem);
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 20px;
  }

  .way-body {
    font-size: 0.9rem;
    line-height: 2;
    color: #555;
    margin-bottom: 32px;
  }

  .way-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 0.8rem;
    font-weight: 500;
    color: #8B6F47;
    letter-spacing: 0.05em;
  }

  /* ============================================================
     CONCEPT
     ============================================================ */
  .layout-text {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
  }

  @media (min-width: 900px) {
    .layout-text {
      grid-template-columns: 260px 1fr;
      gap: 80px;
      align-items: start;
    }
  }

  .text-body p {
    font-size: 1rem;
    line-height: 2.1;
    color: #444;
    margin-bottom: 28px;
  }

  .text-link {
    display: inline-block;
    font-size: 0.875rem;
    font-weight: 500;
    color: #8B6F47;
    letter-spacing: 0.05em;
    border-bottom: 1px solid #8B6F47;
    padding-bottom: 2px;
    transition: opacity 0.3s ease;
  }

  .text-link:hover { opacity: 0.6; }

  /* ============================================================
     PRICING
     ============================================================ */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
    max-width: 760px;
    margin-bottom: 48px;
  }

  @media (min-width: 640px) {
    .pricing-grid { grid-template-columns: 1fr 1fr; }
  }

  .pricing-card {
    border: 1px solid rgba(0,0,0,0.1);
    border-radius: 4px;
    padding: 48px 40px;
    background: #F7F4EF;
  }

  .pricing-card--featured {
    background: #ffffff;
    border-color: #8B6F47;
  }

  .pricing-card-head {
    margin-bottom: 28px;
    padding-bottom: 28px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
  }

  .pricing-plan {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: #8B6F47;
    margin-bottom: 6px;
  }

  .pricing-sub {
    font-size: 0.8rem;
    color: #888;
    letter-spacing: 0.05em;
  }

  .pricing-price {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 5vw, 2.8rem);
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 32px;
  }

  .pricing-tax {
    font-size: 0.8rem;
    font-weight: 400;
    color: #888;
    margin-left: 4px;
  }

  .pricing-includes {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .pricing-includes li {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.875rem;
    color: #444;
    line-height: 1.5;
  }

  .check-icon {
    flex-shrink: 0;
    color: #8B6F47;
  }

  .pricing-notes {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .pricing-notes p {
    font-size: 0.85rem;
    color: #666;
    line-height: 1.7;
  }

  .pricing-notes p::before {
    content: '— ';
    color: #8B6F47;
  }

  /* ============================================================
     HOST PRESENCE
     ============================================================ */
  .host-presence {
    display: flex;
    gap: 20px;
    max-width: 640px;
    margin-top: 64px;
    padding: 32px 36px;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 4px;
    background: #F7F4EF;
  }

  .host-presence-icon {
    flex-shrink: 0;
    color: #8B6F47;
    padding-top: 2px;
  }

  .host-presence-heading {
    font-family: var(--font-heading);
    font-size: 0.95rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 14px;
    letter-spacing: 0.02em;
  }

  .host-presence-text {
    font-size: 0.875rem;
    line-height: 1.9;
    color: #555;
    margin-bottom: 10px;
  }

  .host-presence-note {
    font-size: 0.78rem;
    color: #888;
    line-height: 1.7;
    margin-top: 14px;
  }

  .btn-outline {
    display: inline-block;
    border: 1px solid #8B6F47;
    color: #8B6F47;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    padding: 16px 48px;
    border-radius: 2px;
    transition: background 0.3s ease, color 0.3s ease, transform 0.3s ease;
  }

  .btn-outline:hover {
    background: #8B6F47;
    color: #ffffff;
    transform: translateY(-2px);
  }

  /* ============================================================
     SHOOTING SPEC
     ============================================================ */
  .gallery {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-bottom: 64px;
  }

  @media (min-width: 768px) {
    .gallery { grid-template-columns: 1.6fr 1fr; gap: 16px; }
  }

  .gallery-sub {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }

  @media (min-width: 768px) {
    .gallery-sub { grid-template-columns: 1fr; gap: 16px; }
  }

  .photo-ph {
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    aspect-ratio: 4/3;
  }

  .gallery-main .photo-ph {
    aspect-ratio: unset;
    height: 100%;
    min-height: 260px;
  }

  @media (min-width: 768px) {
    .gallery-main .photo-ph { min-height: 460px; }
    .gallery-sub .photo-ph  { min-height: 215px; }
  }

  .photo-ph span {
    font-family: var(--font-heading);
    font-size: 0.85rem;
    color: rgba(255,255,255,0.8);
    letter-spacing: 0.08em;
  }

  /* photo-placeholder */
  .photo-placeholder {
    position: relative;
    overflow: hidden;
    border-radius: 4px;
  }

  .gallery-main .photo-placeholder {
    height: 100%;
    min-height: 260px;
  }

  @media (min-width: 768px) {
    .gallery-main .photo-placeholder { min-height: 460px; }
    .gallery-sub .photo-placeholder  { min-height: 215px; aspect-ratio: unset; }
  }

  .gallery-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .gallery-main .photo-placeholder .gallery-img,
  .gallery-sub .photo-placeholder .gallery-img {
    position: absolute;
    inset: 0;
  }

  .gallery-sub .photo-placeholder {
    aspect-ratio: 4/3;
  }

  .photo-placeholder::after {
    content: attr(data-caption) " ▶ 要撮影";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: rgba(0,0,0,0.72);
    color: #fff;
    font-size: 11px;
    font-family: var(--font-body);
    padding: 6px 10px;
    letter-spacing: 0.04em;
    z-index: 2;
  }

  .photo-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    background: #cc0000;
    color: #fff;
    font-size: 10px;
    font-family: var(--font-body);
    font-weight: 600;
    padding: 2px 8px;
    border-radius: 2px;
    letter-spacing: 0.08em;
    z-index: 2;
  }

  .offsite-photo {
    aspect-ratio: 4/3;
    min-height: 280px;
  }

  .operator-placeholder {
    aspect-ratio: 1;
    width: 100%;
  }

  .spec-wrap { max-width: 800px; }

  .spec-heading {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 24px;
  }

  .spec-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.875rem;
  }

  .spec-table th,
  .spec-table td {
    text-align: left;
    padding: 16px 20px;
    border-bottom: 1px solid rgba(0,0,0,0.07);
    line-height: 1.7;
    color: #333;
  }

  .spec-table th {
    width: 200px;
    font-weight: 500;
    color: #666;
    white-space: nowrap;
    vertical-align: top;
  }

  @media (max-width: 639px) {
    .spec-table,
    .spec-table tbody,
    .spec-table tr,
    .spec-table th,
    .spec-table td {
      display: block;
      width: 100%;
    }
    .spec-table th {
      padding-bottom: 4px;
      border-bottom: none;
      font-size: 0.75rem;
      letter-spacing: 0.05em;
      color: #999;
    }
    .spec-table td { padding-top: 0; }
  }

  .floorplan-ph {
    margin-top: 48px;
    height: 280px;
    border-radius: 4px;
    border: 1px dashed rgba(0,0,0,0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.02);
  }

  .floorplan-ph p {
    font-size: 0.85rem;
    color: #bbb;
    letter-spacing: 0.1em;
  }

  /* ============================================================
     OFFSITE
     ============================================================ */
  .offsite-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
  }

  @media (min-width: 900px) {
    .offsite-grid { grid-template-columns: 1fr 1fr 1fr; gap: 56px; align-items: start; }
  }

  .offsite-body {
    font-size: 1rem;
    line-height: 2.1;
    color: #444;
    margin-bottom: 40px;
  }

  .offsite-spec {
    display: grid;
    grid-template-columns: 5em 1fr;
    gap: 0;
  }

  .offsite-spec dt {
    font-size: 0.75rem;
    font-weight: 500;
    color: #999;
    letter-spacing: 0.06em;
    padding: 14px 0;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }

  .offsite-spec dd {
    font-size: 0.875rem;
    color: #444;
    line-height: 1.8;
    padding: 14px 0 14px 16px;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }

  .schedule-heading {
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 8px;
  }

  .schedule-note {
    font-size: 0.75rem;
    color: #999;
    letter-spacing: 0.05em;
    margin-bottom: 24px;
  }

  .schedule-footnote {
    font-size: 0.78rem;
    color: #888;
    line-height: 1.7;
    margin-top: 20px;
    padding-top: 16px;
    border-top: 1px solid rgba(0,0,0,0.06);
  }

  .schedule-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .schedule-list li {
    display: flex;
    gap: 24px;
    padding: 16px 0;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }

  .schedule-time {
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    color: #8B6F47;
    white-space: nowrap;
    padding-top: 2px;
    min-width: 40px;
  }

  .schedule-item {
    font-size: 0.875rem;
    color: #444;
    line-height: 1.7;
  }

  /* ============================================================
     CASE STUDIES
     ============================================================ */
  .case-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 32px;
  }

  @media (min-width: 640px) {
    .case-grid { grid-template-columns: repeat(3, 1fr); }
  }

  .case-card { display: flex; flex-direction: column; }

  .case-ph {
    aspect-ratio: 4/3;
    border-radius: 4px;
    background: #e8e4de;
    margin-bottom: 20px;
    /* TODO: 事例写真に差し替え */
  }

  .case-tag {
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #8B6F47;
    margin-bottom: 8px;
  }

  .case-title {
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 10px;
    line-height: 1.5;
  }

  .case-body {
    font-size: 0.85rem;
    color: #666;
    line-height: 1.8;
  }

  /* ============================================================
     OPERATOR
     ============================================================ */
  .operators-wrap {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
  }

  @media (min-width: 768px) {
    .operators-wrap {
      grid-template-columns: 1fr 1fr;
      gap: 56px;
    }
  }

  .operator-card {
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  .operator-ph {
    width: 100%;
    aspect-ratio: 1;
    border-radius: 4px;
    background: #e8e4de;
    /* TODO: 運営者顔写真（/operator.jpg）に差し替え */
  }

  .operator-name {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 4px;
  }

  .operator-role {
    font-size: 0.78rem;
    color: #999;
    letter-spacing: 0.08em;
    margin-bottom: 28px;
  }

  .operator-bio {
    font-size: 0.9rem;
    line-height: 2;
    color: #555;
    margin-bottom: 20px;
  }

  /* ============================================================
     ACCESS
     ============================================================ */
  .access-highlight {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 40px;
    padding: 28px 32px;
    border-left: 3px solid #8B6F47;
    background: rgba(139,111,71,0.04);
  }

  .access-highlight p {
    font-size: 0.95rem;
    color: #1a1a1a;
    line-height: 1.7;
    font-weight: 500;
  }

  .map-toggle {
    display: flex;
    gap: 8px;
    margin-bottom: 28px;
  }

  .map-btn {
    display: inline-block;
    border: 1px solid rgba(0,0,0,0.2);
    color: #555;
    background: transparent;
    font-family: var(--font-body);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    padding: 9px 24px;
    border-radius: 2px;
    cursor: pointer;
    transition: background 0.3s ease, color 0.3s ease, border-color 0.3s ease;
  }

  .map-btn:hover,
  .map-btn--active {
    background: #1a1a1a;
    border-color: #1a1a1a;
    color: #ffffff;
  }

  .map-image-wrap {
    max-width: 900px;
    margin: 0 auto 36px;
  }

  .map-image {
    width: 100%;
    height: auto;
    border-radius: 4px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  }

  .access-info-list {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .access-info-list li {
    font-size: 0.9rem;
    line-height: 1.8;
    color: #444;
    padding: 14px 0;
    border-bottom: 1px solid rgba(0,0,0,0.06);
    padding-left: 1.2em;
    position: relative;
  }

  .access-info-list li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: #8B6F47;
  }

  .area-map-wrap {
    max-width: 900px;
    margin: 40px auto 0;
  }

  .area-map-img {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 4px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  }

  /* ============================================================
     CONTACT
     ============================================================ */
  .contact-inner {
    max-width: 560px;
  }

  .contact-text {
    font-size: 1rem;
    line-height: 2.1;
    color: rgba(255,255,255,0.65);
    margin-bottom: 48px;
  }

  .contact-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-bottom: 56px;
  }

  .btn-primary-light {
    display: inline-block;
    background: #ffffff;
    color: #1a1a1a;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    padding: 16px 40px;
    border-radius: 2px;
    transition: background 0.3s ease, color 0.3s ease, transform 0.3s ease;
  }

  .btn-primary-light:hover {
    background: #8B6F47;
    color: #ffffff;
    transform: translateY(-2px);
  }

  .btn-ghost-light {
    display: inline-block;
    border: 1px solid rgba(255,255,255,0.35);
    color: rgba(255,255,255,0.75);
    font-size: 0.875rem;
    font-weight: 400;
    letter-spacing: 0.08em;
    padding: 16px 40px;
    border-radius: 2px;
    transition: border-color 0.3s ease, color 0.3s ease;
  }

  .btn-ghost-light:hover {
    border-color: rgba(255,255,255,0.7);
    color: #ffffff;
  }

  .contact-legal {
    display: flex;
    flex-wrap: wrap;
    gap: 24px;
  }

  .legal-link {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.35);
    letter-spacing: 0.05em;
    text-decoration: underline;
    text-underline-offset: 3px;
    transition: color 0.3s ease;
  }

  .legal-link:hover { color: rgba(255,255,255,0.7); }

  /* ============================================================
     FOOTER
     ============================================================ */
  .site-footer {
    background: #1a1a1a;
    padding: 64px 0 48px;
  }

  .footer-inner {
    display: flex;
    flex-direction: column;
    gap: 40px;
  }

  .footer-top {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  @media (min-width: 768px) {
    .footer-top {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  .footer-logo-img {
    height: 72px;
    width: auto;
    display: block;
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 28px;
    align-items: center;
  }

  .footer-links a {
    font-size: 0.78rem;
    letter-spacing: 0.1em;
    color: rgba(255,255,255,0.45);
    transition: color 0.3s ease;
  }

  .footer-links a:hover { color: #ffffff; }

  .footer-link-sub {
    font-size: 0.72rem !important;
    color: rgba(255,255,255,0.25) !important;
  }

  .footer-link-sub:hover { color: rgba(255,255,255,0.55) !important; }

  .stay-mini {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 20px 0;
    border-top: 1px solid rgba(255,255,255,0.07);
    border-bottom: 1px solid rgba(255,255,255,0.07);
  }

  .stay-badge {
    display: inline-block;
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    background: rgba(139,111,71,0.6);
    color: #ffffff;
    padding: 4px 12px;
    border-radius: 2px;
    white-space: nowrap;
  }

  .stay-text {
    font-size: 0.78rem;
    color: rgba(255,255,255,0.35);
    letter-spacing: 0.04em;
  }

  .stay-text a {
    color: rgba(255,255,255,0.55);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .footer-copy {
    font-size: 0.72rem;
    color: rgba(255,255,255,0.2);
    letter-spacing: 0.08em;
  }
</style>

<script>
  // Hamburger
  const hamburger = document.querySelector('.hamburger') as HTMLButtonElement | null;
  const mobileMenu = document.getElementById('mobile-menu');

  if (hamburger && mobileMenu) {
    hamburger.addEventListener('click', () => {
      const isOpen = mobileMenu.classList.toggle('open');
      hamburger.classList.toggle('open', isOpen);
      hamburger.setAttribute('aria-expanded', String(isOpen));
      mobileMenu.setAttribute('aria-hidden', String(!isOpen));
    });
    mobileMenu.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        mobileMenu.classList.remove('open');
        hamburger.classList.remove('open');
        hamburger.setAttribute('aria-expanded', 'false');
        mobileMenu.setAttribute('aria-hidden', 'true');
      });
    });
  }

  // Map language toggle
  const mapBtns = document.querySelectorAll('.map-btn');
  const mapImgJa = document.getElementById('map-img-ja');
  const mapImgEn = document.getElementById('map-img-en');

  mapBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      const lang = (btn as HTMLElement).dataset.lang;
      mapBtns.forEach((b) => {
        b.classList.remove('map-btn--active');
        b.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('map-btn--active');
      btn.setAttribute('aria-pressed', 'true');
      if (lang === 'ja') {
        if (mapImgJa) (mapImgJa as HTMLElement).style.display = 'block';
        if (mapImgEn) (mapImgEn as HTMLElement).style.display = 'none';
      } else {
        if (mapImgEn) (mapImgEn as HTMLElement).style.display = 'block';
        if (mapImgJa) (mapImgJa as HTMLElement).style.display = 'none';
      }
    });
  });

  // Fade-in
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.08, rootMargin: '0px 0px -40px 0px' }
  );

  document.querySelectorAll('.fade-in').forEach((el) => observer.observe(el));
</script>
